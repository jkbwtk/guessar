{
  "configurations": [
    {
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "name": "Launch Remote Session",
      "url": "http://localhost:4000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}